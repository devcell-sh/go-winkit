#!/bin/sh
# Example supervised service: heartbeats into the distro's tmp so
# `s6-svstat /etc/s6/services/myagent` shows it up and running.
while true; do
  date > /tmp/myagent-heartbeat
  sleep 30
done
